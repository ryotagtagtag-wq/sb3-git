# sb3-git 使い方ガイド

Scratch `.sb3` プロジェクトを Git/GitHub で共同開発するためのツールキットです。

## 概要

`sb3-git` は以下の機能を提供します：

- **決定論的なインポート/エクスポート** - `.sb3` ↔ 展開形式の相互変換（バイト単位で同一出力）
- **セマンティック ID** - ランダムな UUID ではなく、位置ベースの安定した ID を割り当て
- **ブロックレベルの差分** - 3 段階マッチング（セマンティック ID → コンテンツハッシュ → 未マッチ）で意味のある差分を検出
- **三方向マージ** - 競合検出・自動解決（ours 優先）付き
- **HTML 差分レポート** - scratchblocks SVG でビジュアルな差分表示
- **Git 統合** - `git diff`/`git merge` ドライバ、pre-commit フック、GitHub Actions CI

---

## インストール

```bash
# リポジトリ内で
npm install

# またはグローバルに（推奨）
npm link
```

---

## 基本コマンド

### 1. インポート（.sb3 → 展開形式）

```bash
sb3-git import project.sb3 -o project.expanded
```

出力構造：
```
project.expanded/
├── project.json          # 正規化済みプロジェクト（整形済み JSON）
├── targets/
│   ├── target_0_stage/
│   │   ├── scripts/
│   │   │   └── script_0_0.json
│   │   ├── comments.json
│   │   ├── costumes.json
│   │   └── sounds.json
│   └── target_1_sprite1/
│       └── ...
├── assets/
│   ├── costumes/         # 画像ファイル（MD5 名）
│   └── sounds/           # 音声ファイル（MD5 名）
├── monitors.json
└── extensions.json
```

### 2. エクスポート（展開形式 → .sb3）

```bash
sb3-git export project.expanded project.sb3
```

- **決定論的**: 同じ入力から常にバイト単位で同一の `.sb3` を生成
- アセットは MD5 順で追加されるため、Git での差分が最小化

### 3. 差分レポート生成

```bash
# HTML レポート（ブラウザで開ける）
sb3-git diff base.sb3 modified.sb3 -o diff-report.html

# JSON 形式（プログラム処理用）
sb3-git diff base.sb3 modified.sb3 --json
```

HTML レポートの特徴：
- 色分け：緑＝未変更、青＝追加、赤＝削除、黄＝変更
- 折りたたみ可能なターゲット/スクリプト/ブロック
- scratchblocks SVG でブロックのビジュアル表示
- コピーボタン付きコードブロック

### 4. 三方向マージ

```bash
# 自動解決（ours 優先）
sb3-git merge base.sb3 ours.sb3 theirs.sb3 merged.sb3 --auto-resolve

# 手動解決（競合時に終了コード 1）
sb3-git merge base.sb3 ours.sb3 theirs.sb3 merged.sb3
```

競合がある場合：
```
Conflicts detected: 2
  target_1_sprite1.scripts.script_0_0.blocks.script_0_0_block_5: Block modified in both branches
  target_1_sprite1.costumes.costume_0_backdrop: Asset changed in both branches
```

### 5. 検証

```bash
sb3-git validate project.expanded
```

チェック項目：
- Zod スキーマによる構造検証
- 孤立参照（存在しない ID への参照）
- 重複 ID
- 到達不能ブロック

---

## Git 統合のセットアップ

### 1. `.gitattributes` の配置

リポジトリルートに `.gitattributes` を配置済み：

```
*.sb3 diff=sb3 merge=sb3
```

### 2. Git ドライバの登録

```bash
# diff ドライバ（セマンティック差分表示）
git config diff.sb3.textconv "node scripts/git-diff-sb3"

# merge ドライバ（三方向マージ）
git config merge.sb3.driver "node scripts/git-merge-sb3 %O %A %B %L %P"
```

### 3. pre-commit フック

```bash
cp scripts/pre-commit .git/hooks/pre-commit
chmod +x .git/hooks/pre-commit
```

動作：
- ステージされた `.sb3` ファイルを自動的に展開形式に変換
- `.git/sb3-extracted/` 以下に配置
- 展開されたファイルを自動的に `git add`

### 4. 使い方の流れ

```bash
# 1. 通常通り編集
# Scratch で project.sb3 を編集・保存

# 2. コミット
git add project.sb3
git commit -m "sprite の動きを変更"

# → pre-commit が自動実行され、展開形式もコミットされる

# 3. 差分確認
git diff project.sb3
# → セマンティックな差分（ブロック単位）が表示される

# 4. プルリクエスト作成
# GitHub Actions が自動で：
#   - 検証実行
#   - 決定論的ビルド確認
#   - ベースブランチとの HTML 差分レポート生成（アーティファクトとして保存）
```

---

## GitHub Actions CI

`.github/workflows/ci.yml` が以下を自動実行：

| ジョブ | 内容 |
|--------|------|
| `validate` | 全 `.sb3` ファイルの検証 |
| `deterministic-build` | エクスポートの決定論性確認（2回実行して比較） |
| `diff-report` | PR で変更された `.sb3` の HTML 差分レポート生成・アップロード |
| `merge-test` | 三方向マージのスモークテスト |

---

## 実践的なワークフロー

### チーム開発の例

```
main ブランチ
   │
   ├── feature/movement ───► PR #1 ───► main にマージ
   │       │
   │       └── sprite の移動速度を 10 → 20 に変更
   │
   └── feature/appearance ───► PR #2 ───► main にマージ（競合発生！）
           │
           └── 同じ sprite の色を変更
```

**競合解決の流れ：**

```bash
# feature/appearance で作業中
git checkout feature/appearance
git merge main

# 競合発生時
Auto-merging project.sb3
CONFLICT (content): Merge conflict in project.sb3
Automatic merge failed; fix conflicts and then commit the result.

# 解決
sb3-git merge \
  $(git merge-base main feature/appearance):project.sb3 \
  HEAD:project.sb3 \
  main:project.sb3 \
  project.sb3 --auto-resolve

git add project.sb3
git commit -m "merge main: 外観変更と移動速度変更を統合"
```

---

## トラブルシューティング

### `scratchblocks` の描画エラー

```
Not implemented: HTMLCanvasElement's getContext() method
```

→ `canvas` パッケージが必要な環境では：

```bash
npm install canvas
```

（CI 環境では自動でスキップされ、プレースホルダー表示になります）

### ドライバが動作しない

```bash
# 設定確認
git config --get diff.sb3.textconv
git config --get merge.sb3.driver

# グローバル設定の場合
git config --global diff.sb3.textconv "node /path/to/sb3-git/scripts/git-diff-sb3"
```

### 決定論的ビルドが失敗する

```bash
# 手動テスト
sb3-git export project.expanded /tmp/test1.sb3
sb3-git export project.expanded /tmp/test2.sb3
cmp /tmp/test1.sb3 /tmp/test2.sb3 && echo "OK" || echo "DIFFERENT"
```

差分が出る場合：
- `normalize.ts` のソート順に問題がある可能性
- アセットの収集順序を確認

---

## アーキテクチャ概要

```
.sb3 (ZIP)
    │
    ▼ importSB3()
SB3Project (生の JSON)
    │
    ▼ normalize()
IntermediateProject (セマンティック ID 付き、ブロックをスクリプト構造に変換)
    │
    ▼ assignSemanticIds()
IntermediateProject (全要素に決定論的 ID 付与)
    │
    ├──► expandToFiles() ──► 展開形式（Git 管理用ファイルツリー）
    │
    ├──► diffProjects() ──► ProjectDiff (ブロックレベル差分)
    │       │
    │       └──► generateDiffHTML() ──► HTML レポート
    │
    └──► threeWayMerge() ──► マージ結果 + 競合リスト
            │
            ▼ exportSB3() ──► .sb3 (決定論的 ZIP)
```

---

## ライセンス

MIT License

---

## 参考・関連プロジェクト

- [kubohiroya/sb3-toolchain](https://github.com/kubohiroya/sb3-toolchain) - 決定論的ビルド、インポート/展開、拡張機能管理のリファレンス実装
- [git-sb3](https://github.com/scratchfoundation/git-sb3) - `git-sb3 diff/unpack/pack` コマンド
- [scratch-git](https://github.com/TurboWarp/scratch-git) - TurboWarp 用ユーザースクリプト
- [@mbw/sb3](https://jsr.io/@mbw/sb3) - JSR ライブラリ
